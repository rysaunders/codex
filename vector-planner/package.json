{
  "name": "vector-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:html": "node src/planner.js",
    "build": "npm run build:html && npm run build:pdf",
    "build:pdf": "node scripts/render-pdf.js",
    "check:output": "node scripts/check-output.js",
    "verify": "npm run build:html && npm run check:output"
  },
  "devDependencies": {
    "playwright": "^1.53.0"
  }
}
